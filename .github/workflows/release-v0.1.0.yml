name: Release v0.1.0

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/release-v0.1.0.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout tested source
        uses: actions/checkout@v4
        with:
          ref: 094ec48f013fd3d898a8c058cd67c7474ace4869

      - name: Set up Java 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.10'

      - name: Test and build
        run: gradle clean test build --stacktrace

      - name: Create GitHub release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release create v0.1.0 \
            build/libs/MiraTab-0.1.0.jar \
            --target 094ec48f013fd3d898a8c058cd67c7474ace4869 \
            --title "MiraTab v0.1.0" \
            --notes "Initial MiraTab release for Paper 1.21.11 and Java 21. Adds LuckPerms prefix formatting and primary-group-weight sorting, direct MiraTags API suffix integration with LuckPerms fallback, configurable header/footer placeholders, GLOBAL/WORLD online-count scope, periodic and event-driven refreshes, admin diagnostics, and MiraCore API/health integration."
